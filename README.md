# React + TypeScript + Vite + Vitest + React Testing Library + Husky

This template provides a boilerplate to get a React App working in Vite with pre-configured ESLint, Prettier, Vitest/React Testing Library, unit testing coverage and pre-commit hook with Lint-staged and Husky.

## Usage

```bash
npx degit migodau/vite-react-ts-cq-template my-app

cd my-app
